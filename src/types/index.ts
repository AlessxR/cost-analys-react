export type Status = 'idle' | 'loading' | 'succeeded' | 'failed';

export type {
    ICategory,
    IInitialCategories,
    ICategoryBreakdown,
} from './categories-types';

export type {
    ITransaction,
    INewTransaction,
    IInitialTransaction,
} from './transactions-types';
